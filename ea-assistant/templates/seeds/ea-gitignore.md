# EA Projects — .gitignore template
# Strip this header and the markdown fences before writing to disk.
# The content between the fences is the actual .gitignore.

```gitignore
# Uploaded source documents (large files, client-confidential)
uploads/

# Temporary and generated binary files
tmp/
artifacts/consolidated-report-*.docx
artifacts/executive-pack-*.docx

# OS metadata
.DS_Store
Thumbs.db

# EA workspace config (uncomment the next line to exclude from tracking)
# .ea-workspace.json
```
